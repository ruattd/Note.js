/**
 * Build the website
 */
export function build(args: string[]) {
    console.log(args)
}

/**
 * Start a debug server
 */
export function server(args: string[]) {
}

/**
 * Delete temporary files & directories
 */
export function clean(args: string[]) {
}
